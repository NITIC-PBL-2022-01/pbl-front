import React from "react";
import { Header } from "../../../components/Attendance/Header";

export default {
  title: "Attendance/Header",
  component: Header,
};

export const Default = () => (
  <Header />
);
