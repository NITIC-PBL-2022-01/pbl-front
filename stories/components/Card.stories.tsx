import React from "react";

import { Card } from "../../components/common/Card";

export default {
  title: "Common/Card",
  component: Card,
};

export const Default = () => (
  <Card>Text</Card>
);
